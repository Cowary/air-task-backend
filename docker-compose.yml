version: '3.8'

services:
  air-task-front:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://192.168.1.79:8102}
    restart: unless-stopped
    ports:
      - "8080:80"
    container_name: air-task-front